# Sources to compose into the default output style.
# Each line is a path relative to ~/.claude/. Order matters.
# Frontmatter (if any) is stripped before inclusion.
# Re-run install.sh (or ~/.claude/scripts/compose-output-style.sh) after changes.
skills/caveman/SKILL.md
style/only-use-typeable-characters.md
